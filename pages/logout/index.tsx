import { ConnectedLayout, Logout } from "../../components";

const LogoutPage = () => {
  return (
    <ConnectedLayout title="Cerrando sesión - Company S.A.S.">
      <Logout />
    </ConnectedLayout>
  );
};

export default LogoutPage;
